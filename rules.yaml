preview_settings:
  language: java
##  repository: starwars

default:
  enabled: true
  rules:
    - check if any typo
    - variable naming should be meaningful

## support overriding based on repository
override:
 repository:
   starwars:
     rules:
       - imports should follow alphabetic order

   startrek:
     rules:
       - Use descriptive variable names
       - Use variables even for constants
   ithorian:
     enabled: false       
